{
  "name": "noema-validator",
  "version": "0.1.0",
  "description": "Minimal reference validator for Noema Core objects (non-normative)",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "validate": "node dist/validate.js"
  },
  "dependencies": {
    "ajv": "^8.12.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
